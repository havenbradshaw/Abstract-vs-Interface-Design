Level 1: 

built an abstract class named Shape and gave it attributes (color, name) as well as abstract methods (draw getarea and describe).
built 2 subclasses (circle and rectangle) with inheritance to work with both Interfaces and abstract class. 
built 2 interfaces (calculabe and describable) and implemented them into both circle and rectangle.

pushed my code to GitHub and since there wasn't already a main, I pushed mine as main thinking that just renaming it would be enough. I was wrong. 

AI: I used copilot to aid me in laying out a base plan for what my code should look like as well as catching syntax errors that I made as I went. I used copilots autocomplete feature throughout the code. 

Level 2: 

Added Triangle to work with both interfaces and abstract classes. Further separated the abstract classes and the interfaces to work separately so that they may be tested separately. 

Implemented polymorphism in both abstract classes and interfaces in my methods (draw(), printdescription() etc.)

AI: used copilot's autocomplete feature when re-writing all of my shapes to work solely with the interfaces.

Level 3: 

Had a README already set up, just updating it. 

This comparison between abstract and interfaces is useful to teach early programmers the strengths and weaknesses between abstract classes and interfaces. I learned quite a bit but further testing is needed to grasp which use case is best for each version. 


AI: Used copilot to repeat some of my code to each shape class in interfaces and help me implement code to assign names and colors to each shape. Also used copilot to debug my code. 